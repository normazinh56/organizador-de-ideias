🔹 Métodos de instância (Array.prototype)
<h1>📌 Iteração / Busca</h1>

<p>forEach() – percorre o array</p>

map() – cria novo array transformando os itens

filter() – cria novo array com itens filtrados

find() – retorna o primeiro item que satisfaz a condição

findIndex() – índice do primeiro item encontrado

findLast() – último item que satisfaz a condição

findLastIndex() – índice do último item encontrado

some() – verifica se algum item atende à condição

every() – verifica se todos atendem à condição

reduce() – reduz o array a um único valor

reduceRight() – igual ao reduce, mas da direita para esquerda

<h1>📌 Manipulação (mutam o array)</h1>

push() – adiciona no final

pop() – remove do final

shift() – remove do início

unshift() – adiciona no início

splice() – adiciona/remove/edita elementos

sort() – ordena o array

reverse() – inverte o array

copyWithin() – copia parte do array internamente

fill() – preenche o array com um valor

<h1>📌 Manipulação (não mutam o array – ES2023+)</h1>

toSorted() – retorna array ordenado

toReversed() – retorna array invertido

toSpliced() – versão imutável do splice

with() – substitui um item em um índice específico

<h1>📌 Criação / Combinação</h1>

concat() – combina arrays

slice() – extrai parte do array

flat() – achata arrays

flatMap() – map + flat

join() – transforma em string

at() – acessa índice (aceita negativo)

<h1>📌 Verificação / Busca por índice</h1>

includes() – verifica se contém valor

indexOf() – primeiro índice do valor

lastIndexOf() – último índice do valor

<h1>📌 Iteradores</h1>

entries() – pares [índice, valor]

keys() – índices

values() – valores

@@iterator
 – mesmo que values()

<h1>📌 Conversão para string</h1>

toString()

toLocaleString()

🔹 Métodos estáticos (Array)

Array.from() – cria array a partir de iterable

Array.isArray() – verifica se é array

Array.of() – cria array com os argumentos

🔹 Propriedades

length – tamanho do array

<h1>bons para aprender </h1>
map, filter, reduce, find, some, every, includes, sort
